import { Routes } from "@angular/router";
import { LoginComponent } from "./login.component";

export default [
    {
        path: '',
        title: 'Login',
        component: LoginComponent,
    }
] as Routes